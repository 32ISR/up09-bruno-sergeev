info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: https://kitek.ktkv.dev/feedback/api/auth/login
  body:
    type: json
    data: |-
      {
          "username": "Admin",
          "password": "AdminAccess"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
