info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: https://kitek.ktkv.dev/feedback/api/auth/register
  body:
    type: json
    data: |-
      {
          "username": "Admin",
          "password": "AdminAccess",
          "email": "Admin@mail.ru"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
